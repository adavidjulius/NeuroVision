# modules/depth.py — Phase 4 stub

class DepthEstimator:
    def __init__(self):
        print("⏳  DepthEstimator: not yet implemented (Phase 4)")

    def estimate(self, frame):
        """Returns depth map array (same HxW as frame)."""
        return None

    def get_distance(self, depth_map, box) -> float:
        """Returns relative distance for a bounding box region."""
        return 0.0
